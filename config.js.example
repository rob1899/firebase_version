// Google OAuth configuration.
// Copy this file to config.js and fill in your values.
// config.js is excluded from version control.
const APP_CONFIG = {
    // OAuth 2.0 Client ID from Google Cloud Console
    // Restrict this to your Firebase Hosting domain in the Cloud Console.
    CLIENT_ID: 'YOUR_GOOGLE_OAUTH_CLIENT_ID',

    // Optional: pre-fill the Google sign-in prompt with a specific account.
    // Remove or leave empty to allow any Google account.
    LOGIN_HINT: '',
};
